import styled from 'styled-components';

const StyledHeader = styled.h2`
  padding: 1em;
`;

const StyledHeaderLight = styled.h2`
  padding: 0.5em;
  color: #fff;
`;

export { StyledHeader, StyledHeaderLight };
